<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EJS is runnig</title>
</head>
<body>
    <h1> <%= title %>  </h1>
    <%= new Date().toString() %>
    <% if (post.published) { %>
        <h1><%= post.title %></h1>
        <p><%= post.description %></p>
    <% } else { %>
        <p><%= post.err_massage %></p>
    <% } %>
    <ul>
        <% for( let p of student) { %>
            <li>
                Name:<%= p.name %> || role:<%= p.role %> 
            </li>
        <% } %>
    </ul>
</body>
</html>